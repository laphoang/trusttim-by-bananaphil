[project]
name = "trusttim-chunking"
version = "0.1.0"
description = "Structure-aware recursive chunking of cleaned PDF markdown into KB-ready chunks."
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34,<2",
    "tiktoken>=0.7,<1",
    "langchain-text-splitters>=0.3,<1",
]
