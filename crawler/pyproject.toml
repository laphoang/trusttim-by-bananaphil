[project]
name = "trusttim-crawler"
version = "0.1.0"
description = "Deep-crawl benhvientimhanoi.vn to S3 as markdown + metadata for later KB chunking."
requires-python = ">=3.10"
dependencies = [
    "crawl4ai>=0.5,<0.8",
    "boto3>=1.34,<2",
    "httpx>=0.27,<1",
]
