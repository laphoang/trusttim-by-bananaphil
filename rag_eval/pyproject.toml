[project]
name = "trusttim-rag-eval"
version = "0.1.0"
description = "RAGAS-based retrieval + answer quality evaluation for TrustTim, judged by gpt-5.5."
requires-python = ">=3.10"
dependencies = [
    "ragas>=0.2,<0.3",
    "langchain-openai>=0.2,<1",
    "requests>=2.31,<3",
]
