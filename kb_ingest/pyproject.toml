[project]
name = "trusttim-kb-ingest"
version = "0.1.0"
description = "Embed S3 knowledge-base chunk JSONL and upsert into Supabase's kb_chunks table."
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34,<2",
    "requests>=2.31,<3",
    "psycopg[binary]>=3.1,<4",
]
